export type SuccessfulResponse = {
  title: string;
  message: string;
};

export type ErrorFormResponse = {
  message: string;
};

// Možda će biti drukčiji output u budučnosti pa ostavi ovako
export type ErrorToastResponse = {
  title: string;
  message: string;
};
